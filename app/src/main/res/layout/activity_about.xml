<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.example.jnguyen.slancho.AboutActivity">

    <ImageButton
        android:id="@+id/about_image_button"
        android:layout_width="match_parent"
        android:layout_height="350dp"
        android:layout_marginTop="8dp"
        android:layout_weight="1"
        android:onClick="openLinkedInProfile"
        android:scaleType="fitXY"
        android:src="@drawable/img_creator"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/about_description"
        style="@style/TextAppearance.AppCompat.Title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAlignment="center"
        android:paddingTop="8dp"
        app:layout_constraintTop_toBottomOf="@+id/about_image_button"
        tools:text="@string/about_description" />
</android.support.constraint.ConstraintLayout>
